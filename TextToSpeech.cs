using UnityEngine;

public class TextToSpeech : MonoBehaviour
{
    public void Speak(string text)
    {
        // Implement the text-to-speech functionality here
        // For example, you might use Unity's text-to-speech capabilities or an external plugin
        Debug.Log("Speaking: " + text);
    }
}
